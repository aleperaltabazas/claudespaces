# Drop multiple containers per project

After some use, I see no benefit for a given project or projects to have separate containers instead of a single one, given that new conversations can be created in the claude CLI.