# Additional mounts

Allow users to add custom files to mount to the container via claudespaces config. This should be both global and project. For example, ~/.gitconfig can be loaded into the container to mirror the user's git config inside and outside the container for all projects.