# Claudespaces Container

Your projects are mounted as subdirectories of `/workspace`. Each subdirectory is an independent project — look there for project-specific `CLAUDE.md` instructions and source code.

Start by listing `/workspace` to see which projects are available.
